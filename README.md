# Autonomous Operations Agent (MCP)

A monorepo project for an autonomous operations agent using the Model Context Protocol (MCP).

## Structure

- `web/` - Next.js UI + API routes
- `mcp-servers/` - Python MCP servers

## Setup

Setup instructions will be added in a future update.